services:
  app:
    image: librarylite:0.1
    ports:
      - "${APP_PORT:-8000}:8000"
    environment:
      - DATABASE_URL=sqlite:////data/app.db
    volumes:
      - db_data:/data

volumes:
  db_data:
